import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  mainContainer: {
    marginTop: `10%`,
    paddingHorizontal: `5%`,
  },
  mainTitle: {
    fontSize: 24,
    fontWeight: '600',
  },
});
